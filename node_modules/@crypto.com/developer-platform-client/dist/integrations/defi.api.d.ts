import { DefiProtocol, Farm, WhitelistedToken } from '../lib/client/interfaces/defi.interfaces.js';
import { ApiResponse } from './api.interfaces.js';
/**
 * Fetches whitelisted tokens for a specific DeFi protocol.
 */
export declare const getWhitelistedTokens: (protocol: DefiProtocol) => Promise<ApiResponse<WhitelistedToken[]>>;
/**
 * Fetches all farms for a specific DeFi protocol.
 */
export declare const getAllFarms: (protocol: DefiProtocol) => Promise<ApiResponse<Farm[]>>;
/**
 * Fetches a specific farm by its symbol for a DeFi protocol.
 */
export declare const getFarmBySymbol: (protocol: DefiProtocol, symbol: string) => Promise<ApiResponse<Farm>>;
