export declare enum ChainName {
    CRONOS_EVM = "25",
    CRONOS_EVM_TESTNET = "338",
    CRONOS_ZKEVM = "388",
    CRONOS_ZKEVM_TESTNET = "240"
}
export interface Chain {
    id: number;
    name: ChainName;
    explorerUrl: string;
    rpc: string;
}
export declare const CHAIN: Record<number, ChainName>;
export declare const EXPLORER_URLS: Record<ChainName, string>;
export declare const DEFAULT_RPC_URLS: Record<ChainName, string>;
export declare const CHAIN_INFO: Record<ChainName, Chain>;
export declare class CronosId {
    private static readonly registryContracts;
    static isCronosId(name: string): boolean;
    static isSupportedChain(chainId: string): boolean;
    static forwardResolve(cronosId: string, chainId: string): Promise<string>;
    static reverseResolve(address: string, chain: Chain): Promise<string>;
}
