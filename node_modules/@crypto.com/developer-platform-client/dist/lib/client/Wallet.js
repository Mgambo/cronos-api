"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Wallet = void 0;
const wallet_api_js_1 = require("../../integrations/wallet.api.js");
const Client_js_1 = require("./Client.js");
/**
 * Wallet class handles operations related to wallet creation and balance retrieval.
 *
 * @class
 */
class Wallet {
    /**
     * Creates a new wallet using the API.
     *
     * @async
     * @returns {Promise<ApiResponse<CreateWalletData>>} - A promise that resolves to the newly created wallet details.
     * @throws {Error} - Throws an error if the request fails.
     *
     * @example
     * const newWallet = await Wallet.create();
     * console.log(newWallet);
     */
    static async create() {
        return await (0, wallet_api_js_1.createWallet)();
    }
    /**
     * Retrieves the balance of the wallet for a specific address.
     *
     * @async
     * @param {string} address - The wallet address to fetch the balance for (CronosIds with the `.cro` suffix are supported, e.g. `XXX.cro`)
     * @returns {Promise<ApiResponse<Balance>>} - A promise that resolves to the balance of the wallet.
     * @throws {Error} - Throws an error if the request fails.
     *
     * @example
     * const balance = await Wallet.balance('0x...');
     * console.log(balance);
     */
    static async balance(address) {
        const chainId = Client_js_1.Client.getChainId();
        const apiKey = Client_js_1.Client.getApiKey();
        return await (0, wallet_api_js_1.getBalance)(chainId, address, apiKey);
    }
}
exports.Wallet = Wallet;
